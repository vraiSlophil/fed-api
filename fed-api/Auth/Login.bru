meta {
  name: Login
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/login
  body: json
  auth: inherit
}

headers {
  X-XSRF-TOKEN: {{xsrf_token}}
}

body:json {
  {
    "email": "test@example.com",
    "password": "password"
  }
}
